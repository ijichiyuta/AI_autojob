-- ============================================================
-- スコア重みの変更: 応募数の少なさを事実上の足切りにしない
--
-- 本人判断（2026-09-12）。応募数の多い案件も応募対象に入れたい。
--
-- 応募数はNG判定には使っていなかったが、重み15を満額失うため
-- （応募20人超はほぼ0点）、1日10件の応募上限とスコア順のレビューを前提にすると
-- 事実上そこまで届かなかった。重みを 15 → 5 に下げる。
--
-- 空いた10点は時給換算に寄せる（30 → 40）。週20時間しか動けない以上、
-- 「低単価案件で工数だけ溶ける」（要件定義8章の最大リスク）が最も効く指標のため。
--
-- 応募多数であること自体は high_competition の警告フラグで 人間に見えるようにしてある。
-- ============================================================
update public.settings
set value = '{
  "hourly_rate":      40,
  "client_quality":   20,
  "low_competition":   5,
  "specificity":      15,
  "genre_match":      10,
  "deadline_margin":  10
}'::jsonb
where key = 'scoring_weights';
